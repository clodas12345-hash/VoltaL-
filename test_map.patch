--- src/components/MapComponent.tsx
+++ src/components/MapComponent.tsx
@@ -38,6 +38,8 @@
   isDemoMode: boolean;
   onEnableDemo: () => void;
   focusLocationTrigger?: { lat: number; lng: number; zoom: number; timestamp: number } | null;
+  pendingPin?: { lat: number; lng: number } | null;
+  onPendingPinDragEnd?: (latLng: { lat: number; lng: number }) => void;
 }
 
 const CATEGORY_COLORS: Record<PlaceCategory, string> = {
@@ -248,6 +250,22 @@
             </div>
           </AdvancedMarker>
         )}
+        
+        {/* Pending Custom Pin */}
+        {pendingPin && (
+          <AdvancedMarker 
+            position={pendingPin} 
+            draggable={true}
+            onDragEnd={(e) => {
+              if (e.latLng && onPendingPinDragEnd) {
+                onPendingPinDragEnd({ lat: e.latLng.lat, lng: e.latLng.lng });
+              }
+            }}
+            zIndex={100}
+          >
+            <div className="relative -top-6">
+              <div className="w-12 h-12 bg-blue-600 rounded-full flex items-center justify-center shadow-xl animate-bounce border-4 border-white">
+                <PinIcon className="w-6 h-6 text-white" />
+              </div>
+              <div className="absolute -bottom-2 left-1/2 -translate-x-1/2 w-4 h-2 bg-black/40 rounded-full blur-[2px]" />
+            </div>
+          </AdvancedMarker>
+        )}
 
         {/* Saved Favorite Places Markers */}
         {visibleSavedPlaces.map((place) => {
