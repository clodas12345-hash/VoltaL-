--- src/components/Header.tsx
+++ src/components/Header.tsx
@@ -1,6 +1,6 @@
 import React, { useState } from 'react';
 import { Search, Navigation, Bookmark, MapPin, Plus, Compass } from 'lucide-react';
 import { PlaceCategory } from '../types';
 
 interface HeaderProps {
   searchQuery: string;
@@ -9,16 +9,8 @@
   savedCount: number;
   onOpenSaved: () => void;
   selectedCategoryFilter: PlaceCategory | 'Todos';
   onSelectCategoryFilter: (category: PlaceCategory | 'Todos') => void;
   onOpenAddCustomPin: () => void;
+  categories: PlaceCategory[];
+  onAddCategory: (newCategory: string) => void;
 }
-
-const CATEGORIES: (PlaceCategory | 'Todos')[] = [
-  'Todos',
-  'Padaria',
-  'Restaurante',
-  'Cafeteria',
-  'Supermercado',
-  'Farmácia',
-  'Shopping',
-  'Outros'
-];
 
 export function Header({
   searchQuery,
@@ -28,8 +20,11 @@
   selectedCategoryFilter,
   onSelectCategoryFilter,
   onOpenAddCustomPin,
+  categories,
+  onAddCategory
 }: HeaderProps) {
   const [isFocused, setIsFocused] = useState(false);
+  const allCategories = ['Todos', ...categories];
 
   return (
     <div className="absolute top-0 left-0 w-full z-10 px-4 pt-6 pb-2 bg-gradient-to-b from-white/90 via-white/70 to-transparent pointer-events-none">
@@ -73,7 +68,7 @@
       <div className="flex gap-2 overflow-x-auto pb-2 -mx-4 px-4 scrollbar-hide pointer-events-auto snap-x">
-        {CATEGORIES.map((cat) => {
+        {allCategories.map((cat) => {
           const isSelected = selectedCategoryFilter === cat;
           return (
             <button
               key={cat}
